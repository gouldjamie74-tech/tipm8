# Local build/deploy scratch
.netlify/
node_modules/

# Source archive — the unpacked files are the source of truth
*.zip

# OS / editor
Thumbs.db
.DS_Store
.vscode/

# Exports written into the project folder by the app itself — shift data, never committed
tipm8-*.csv
blendm8-*.csv
tipm8-backup-*.json
